# Test properties — use your local MySQL test database
# Replace the URL/username/password below with your MySQL test DB values.
spring.datasource.url=jdbc:mysql://localhost:3306/dia_assist_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Let Hibernate create/drop schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Use the actual database for @DataJpaTest (do not replace with in-memory DB)
spring.test.database.replace=NONE
